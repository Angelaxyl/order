<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.orderapi.mapper.BillMapper">

    <select id="pageCC" resultType="com.orderapi.entity.BillRes">
        select bill.*,
               (select u.name from user u where u.id=bill.user_id)userName
        from bill
            ${ew.customSqlSegment}
        order by (create_time) desc
    </select>

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.orderapi.entity.Bill">
        <id column="id" property="id" />
        <result column="order_id" property="orderId" />
        <result column="user_id" property="userId" />
        <result column="total" property="total" />
        <result column="bill_state" property="billState" />
        <result column="bill_url" property="billUrl" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, order_id, user_id, total, bill_state, bill_url, create_time, update_time
    </sql>

</mapper>
