<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.orderapi.mapper.OutDevicesForSimMapper">

    <select id="findByOrderId" resultType="com.orderapi.entity.OutDevicesForSimRes">
        select a.*,b.device_name deviceName
        from out_devices_for_sim a ,device b
        where a.order_id = #{orderId} and
            a.device_id = b.id
    </select>


    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.orderapi.entity.OutDevicesForSim">
        <id column="id" property="id" />
        <result column="device_id" property="deviceId" />
        <result column="order_id" property="orderId" />
        <result column="sim" property="sim" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, device_id, order_id,sim, create_time, update_time
    </sql>

</mapper>
